#include<stdio.h>
int main(){
	int a(100),i,n,k;
	printf("enter the number of elements");
	scanf("%d",&n);
	for(i=0;i<n;i++);
	scanf("%d",&a);
	printf("enter the element to search");
	scanf("%d",&k);
	for(i=0;i<n;i++);
	{
		if(a==k){
		printf("positiion of %d is %d,k,i+l");
		}
	}
	return 0;
}
